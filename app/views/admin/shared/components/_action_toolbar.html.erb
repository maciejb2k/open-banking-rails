<%# locals: (left: nil, right: nil) — slots accept HTML-safe strings (built via `capture do ... end`). %>
<%# Either may be omitted; groups wrap independently on mobile. See styleguide for examples. %>
<div class="flex flex-wrap items-center justify-between gap-2">
  <div class="flex flex-wrap items-center gap-2">
    <%= left %>
  </div>
  <div class="flex flex-wrap items-center gap-2">
    <%= right %>
  </div>
</div>
