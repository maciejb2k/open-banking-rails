<%# locals: (left: nil, right: nil) - slots are HTML-safe strings (use `capture do ... end`). %>
<div class="flex flex-wrap items-center justify-between gap-2">
  <div class="flex flex-wrap items-center gap-2">
    <%= left %>
  </div>
  <div class="flex flex-wrap items-center gap-2">
    <%= right %>
  </div>
</div>
