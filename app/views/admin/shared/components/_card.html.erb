<%# locals: (title: nil, subtitle: nil, padding: true, actions: nil) %>
<%# `title:` only works with padding=true. With padding=false, hand-roll a `<div class="border-b px-4 py-3">` header inside the block. %>
<div class="admin-section <%= padding ? "" : "p-0 overflow-hidden" %>">
  <% if title && padding %>
    <div class="mb-4 flex items-start justify-between gap-3">
      <div>
        <h2 class="text-lg font-semibold text-foreground"><%= title %></h2>
        <% if subtitle %>
          <p class="text-sm text-muted-foreground"><%= subtitle %></p>
        <% end %>
      </div>
      <% if actions %>
        <div class="shrink-0"><%= actions %></div>
      <% end %>
    </div>
  <% end %>
  <%= yield %>
</div>
