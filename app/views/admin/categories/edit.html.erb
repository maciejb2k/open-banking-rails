<div class="space-y-6">
  <%= render "admin/shared/components/page_header",
        title: @category.name,
        subtitle: @category.path.to_s.count(".").zero? ? "Top-level domain" : "Path: #{@category.path}",
        back_path: admin_categories_path %>

  <%= render "admin/categories/form", category: @category %>
</div>
